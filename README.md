### 🚨 실행 전 주의사항 (Prerequisites)
* 커스텀 사물 인식 모델을 실행하기 위해 `null.pt` 파일이 반드시 필요합니다. (파일명마다 어떤 객체 인식하는지 구분되게 적어놓았습니다.)
* 깃허브 용량 제한으로 업로드되지 않았으니, 별도로 전달받은 `null.pt`를 `Sensor/` 폴더 안에 넣어주세요.
* (참고: 기본 모델인 `yolov8n.pt`는 파일이 없어도 코드 실행 시 자동으로 다운로드됩니다.)
